name: flutter_mobx_study
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Adicionamos aqui os pacotes do mobx
  mobx:
  flutter_mobx:

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Adicionamos aqui as dependencias de desenvolvimento
  build_runner:
  mobx_codegen:

flutter:
  uses-material-design: true
